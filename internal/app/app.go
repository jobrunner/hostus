package app

import "context"

// Run starts and runs the hostus service until ctx is done or an
// unrecoverable error occurs. It is currently a stub filled in by later tasks.
func Run(ctx context.Context) error {
	return nil
}
